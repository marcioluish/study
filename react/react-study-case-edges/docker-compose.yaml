version: "3.8"

services:
  cedges:
    container_name: cedges
    image: cedges
    build:
      context: ./app
      dockerfile: Dockerfile
    volumes:
      # bind mount
      - ./app:/app
      # named mount log
      - logs:/app/logs
      # anonymous mount
      - /app/node_modules
    env_file:
      - ./app/.env
    ports:
      - 3000:3000

volumes:
  logs:
